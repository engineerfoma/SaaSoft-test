# SaaSoft Test - Система управления учетными записями

Vue.js приложение для управления учетными записями с валидацией и локальным хранением данных.

## 🚀 Технологии

- **Vue 3** с Composition API
- **TypeScript** для типизации
- **Pinia** для управления состоянием
- **Vite** для сборки
- **SCSS** для стилизации
- **UIKit** для UI компонентов

## 📁 Структура проекта

```
src/
├── entities/           # Бизнес-сущности
│   └── account/        # Типы и интерфейсы аккаунтов
├── shared/            # Общие компоненты и утилиты
│   ├── stores/        # Pinia stores
│   ├── styles/        # Глобальные стили
│   └── types/         # Общие типы
├── widgets/           # Виджеты (крупные компоненты)
│   └── AccountsList/  # Список аккаунтов
│       ├── components/    # Компоненты виджета
│       └── inputs/        # Поля ввода
├── pages/             # Страницы
└── app/              # Конфигурация приложения
```

## 🎯 Функциональность

### Управление аккаунтами
- ✅ Создание новых учетных записей
- ✅ Редактирование существующих записей
- ✅ Удаление записей
- ✅ Переключение видимости пароля

### Типы аккаунтов
- **Локальная** - требует логин и пароль
- **LDAP** - требует только логин

### Валидация данных
- **Логин**: обязательное поле, максимум 100 символов
- **Пароль**: обязательное для локальных аккаунтов, максимум 100 символов
- **Метки**: необязательное поле, максимум 50 символов

### Особенности UX
- 🔄 **Отложенное сохранение** - данные сохраняются только при потере фокуса (blur)
- ⚠️ **Валидация в реальном времени** - красная обводка при ошибках
- 🧹 **Автоочистка** - невалидные данные очищаются из стейта
- 🔄 **Синхронизация** - локальное состояние синхронизируется с глобальным стейтом

## 🛠 Установка и запуск

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build

# Предварительный просмотр сборки
npm run preview
```

## 📝 Использование

### Создание аккаунта
1. Нажмите кнопку "Добавить учетную запись"
2. Заполните поля:
   - **Метки** (необязательно) - через точку с запятой
   - **Тип записи** - выберите из списка
   - **Логин** - обязательное поле
   - **Пароль** - только для локальных аккаунтов
3. Поля сохраняются автоматически при потере фокуса

### Валидация
- Пустые обязательные поля подсвечиваются красным
- При ошибке валидации поле очищается
- Валидные данные сохраняются в стейт

### Управление паролями
- Нажмите на иконку "глаза" для показа/скрытия пароля
- Поле пароля автоматически очищается при смене типа на LDAP

## 🏗 Архитектура

### Управление состоянием
- **Pinia Store** (`accounts.ts`) - централизованное хранение данных
- **SessionStorage** - персистентность между сессиями
- **Локальное состояние** - временное хранение при редактировании

### Компонентная структура
- **AccountsList** - главный виджет со списком
- **AccountRow** - строка с данными аккаунта
- **Input компоненты** - специализированные поля ввода
- **Валидация** - централизованная в store

### Принципы разработки
- **Feature-Sliced Design** - организация по доменам (гибридный подход для дальнейшего масштабирования, переиспользуемые компоненты переносятся в shared/ui итд)
- **Composition API** - современный подход Vue 3
- **TypeScript** - строгая типизация
- **Реактивность** - `reactive()` для объектов, `ref()` для примитивов

## 🎨 Стилизация

- **CSS Custom Properties** - для цветов и размеров
- **SCSS** - для вложенности и миксинов
- **UIKit** - для базовых компонентов
- **Responsive** - адаптивный дизайн (до планшета)

## 🔧 Конфигурация

### Vite
- TypeScript поддержка
- SCSS препроцессор
- Алиасы путей (`@/` для src)

### TypeScript
- Строгий режим
- Vue типизация
- Путевые алиасы

## 📊 Производительность

- **Оптимизированная реактивность** с `reactive()`
- **Минимальные перерендеры** благодаря правильной структуре данных
- **Локальное хранение** для быстрого доступа

## 🚀 Возможные улучшения

- [ ] Серверная валидация
- [ ] Drag & Drop для сортировки
- [ ] Поиск и фильтрация
- [ ] Экспорт/импорт данных
- [ ] Темная тема
- [ ] PWA поддержка